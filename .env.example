# Copy to .env and fill in values for local testing only (do NOT commit .env)
# For emulator usage:
FIRESTORE_EMULATOR_HOST=localhost:8080
GCLOUD_PROJECT=your-firebase-project-id

# For cloud usage (NOT recommended to store in .env for production):
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json
